package sql_practical_challenge;

public class Task3 {
	
	/*
	 The website has to generate an invoice number using following logic for the order placed in 2023
	 invoice number first two letter of username + first two digit of order id + last 2 digit of product id
	 + last 2 digit of mobile number.
	 The result set should have columns: name, invoice number and created_at and should be sorted in descending order
	 by created_at.
	 */

}
